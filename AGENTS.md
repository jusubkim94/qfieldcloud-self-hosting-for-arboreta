# 프로젝트 목적

- 이 저장소는 QFieldCloud를 AWS에 셀프호스팅하기 위한 독립적인 비공식 배포 자동화 프로젝트이다.
- 이 프로젝트의 핵심 결과물은 누구나 재현할 수 있는 QFieldCloud 자동 구축 도구이다.
- 수목원 식물이력관리시스템은 이 자동 구축 도구의 활용 사례이지만, 이 저장소의 유일한 목적은 아니다.
- 명시적인 요청이 없으면 QFieldCloud 공식 소스 코드를 포크하거나 수정하지 않는다.
- 검증된 QFieldCloud 공식 릴리스와 컨테이너 이미지를 사용한다.

# 사용자와의 의사소통

- 저장소 소유자는 Git, GitHub, AWS 및 웹 개발을 처음 접하는 초보자이다.
- 중요한 결정과 작업 결과를 쉬운 한국어로 설명한다.
- 전문용어나 약어는 처음 사용할 때 뜻을 함께 설명한다.
- 사용자가 직접 수행해야 하는 작업은 클릭 위치와 입력 내용을 단계별로 안내한다.
- 중요한 아키텍처를 변경하기 전에 목적, 예상 비용, 위험요소 및 복구 방법을 설명한다.
- 불확실한 내용을 사실처럼 단정하지 말고, 확인된 내용과 추정한 내용을 구분한다.

# 안전 원칙

- AWS 접근키, 비밀번호, API 키, 개인키 및 기타 인증정보를 요청하거나 출력하거나 저장소에 커밋하지 않는다.
- 소스 코드, 예제 파일, 문서 및 로그에 인증정보를 포함하지 않는다.
- 사용자의 명시적인 승인을 받지 않고 비용이 발생하는 AWS 자원을 생성·변경·삭제하지 않는다.
- 기존 식물이력관리용 PostGIS 데이터베이스를 변경하지 않는다.
- QFieldCloud 운영 데이터베이스와 식물이력관리용 PostGIS 데이터베이스를 별개의 시스템으로 취급한다.
- 운영용 의존성에는 `latest`, `master`, `main`처럼 버전이 고정되지 않은 참조를 사용하지 않는다.
- 공식 릴리스 버전, 커밋 해시 또는 컨테이너 이미지 다이제스트처럼 재현 가능한 버전을 사용한다.
- `curl | bash`처럼 인터넷에서 내려받은 스크립트를 즉시 실행하는 설치 방식을 사용하지 않는다.
- `main` 브랜치에 직접 커밋하지 않는다. 기능 브랜치에서 작업하고 Pull Request를 통해 변경사항을 제안한다.
- 파괴적인 명령이나 데이터 삭제가 필요한 경우 실행 전에 대상, 영향 및 복구 가능성을 설명하고 사용자의 승인을 받는다.

# 배포 아키텍처

다음 두 가지 배포 방식을 지원하는 것을 목표로 한다.

## lab-lightsail

- 저비용의 실험·학습·소규모 운영을 위한 AWS Lightsail 기반 배포 방식이다.
- QFieldCloud 구성요소를 가능한 한 단일 서버에서 실행한다.
- 운영 안정성보다 단순한 설치와 낮은 비용을 우선한다.
- 단일 서버 장애 시 전체 서비스가 중단될 수 있음을 문서에 명확히 표시한다.

## standard-aws

- 실제 운영과 확장성을 고려한 표준 AWS 배포 방식이다.
- 애플리케이션 실행에는 EC2를 사용한다.
- QFieldCloud 운영 데이터베이스에는 PostgreSQL/PostGIS를 지원하는 RDS를 사용한다.
- 프로젝트 파일과 첨부파일 저장에는 S3 또는 검증된 S3 호환 저장소를 사용한다.
- 이메일 발송이 필요하면 AWS SES 등 관리형 이메일 서비스를 우선 사용한다.
- 명시적인 필요가 없으면 별도의 메일서버를 직접 구축하거나 운영하지 않는다.

# 구현 원칙

- 가능하면 Infrastructure as Code 방식으로 AWS 자원을 정의한다.
- 자동화 도구를 선택하기 전 Terraform, OpenTofu, AWS CloudFormation 등의 장단점을 비교하여 설명한다.
- 설치 과정은 반복 실행해도 기존 자원이 불필요하게 중복 생성되지 않도록 설계한다.
- 운영 환경과 실험 환경의 설정을 분리한다.
- 설정값은 예제 파일로 제공하고 실제 비밀번호와 인증정보는 포함하지 않는다.
- 사용자가 AWS 계정을 만든 후 최소한의 입력만으로 설치할 수 있는 구조를 목표로 한다.
- 설치뿐만 아니라 업데이트, 백업, 복원 및 제거 절차도 함께 제공한다.
- QFieldCloud의 공식 라이선스와 사용 중인 외부 구성요소의 라이선스를 준수한다.
- 이 저장소가 QField 또는 QFieldCloud 공식 프로젝트가 아닌 비공식 프로젝트임을 README에 명확히 표시한다.

# 검증 원칙

- 작업 완료를 선언하기 전에 설정 파일과 스크립트를 검증한다.
- 가능한 경우 코드 검사, 자동 테스트, 배포 전 검증 및 서비스 상태 확인 기능을 추가한다.
- 실제로 실행하지 않은 작업을 성공했다고 표현하지 않는다.
- 테스트가 완료된 기능, 아직 검증되지 않은 가정 및 향후 작업을 명확히 구분한다.
- 배포 변경에는 가능한 경우 되돌리기 방법을 함께 작성한다.
- 비용이 발생할 수 있는 자원은 예상 비용과 비용 중단 방법을 문서화한다.

# Git 및 GitHub 작업 원칙

- 한 번의 변경에는 하나의 명확한 목적만 포함한다.
- 커밋 메시지는 변경 이유를 알 수 있도록 작성한다.
- 작업 전 현재 브랜치와 변경사항을 확인한다.
- 사용자와 관계없는 기존 파일이나 변경사항을 수정하거나 삭제하지 않는다.
- 기능 브랜치 이름은 작업 내용을 알 수 있도록 작성한다.
- Pull Request 설명에는 변경 목적, 주요 변경사항, 검증 결과, 비용 영향 및 복구 방법을 포함한다.
- 초보 사용자가 검토할 수 있도록 변경된 파일과 의미를 한국어로 설명한다.
